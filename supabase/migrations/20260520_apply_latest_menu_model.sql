-- This exact migration version/name already exists in the remote Supabase
-- migration history from an earlier GitHub run. Keep this no-op file so the
-- remote history can reconcile with the local migrations directory.
-- The latest idempotent schema/data update lives in:
-- 20260520000100_apply_latest_menu_model.sql
select 1;
